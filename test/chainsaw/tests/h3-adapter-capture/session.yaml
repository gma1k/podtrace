apiVersion: podtrace.io/v1alpha1
kind: PodTraceSession
metadata:
  name: h3-curl-session
spec:
  selector:
    matchLabels:
      app: h3curl
  duration: 30s
  filters: [net]
  exporterRef:
    name: h3-otlp
  reportRef:
    configMap:
      name: h3-curl-report
