apiVersion: podtrace.io/v1alpha1
kind: PodTraceSession
metadata:
  name: http-session
spec:
  selector:
    matchLabels:
      app: http-target
  duration: 20s
  filters: [net]
  exporterRef:
    name: http-otlp
  reportRef:
    configMap:
      name: http-report
